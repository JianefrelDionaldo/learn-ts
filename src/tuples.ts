// Tuple - TypeScript has a new type called Tuple.
//         Which is a fixed length array where each element has a particular type.
//         We often use them when working with a pair of values.

// A id = 1 and a name = 'Mosh'
let users: [number, string] = [11, 'Mosh'];
